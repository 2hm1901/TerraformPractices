# ============================================================
# Week1-Lab1: EC2 + minikube + ALB
#
# Providers:
#   aws        — EC2, ALB, Security Groups, VPC
#   kubernetes — deploy nginx vao minikube
#   tls        — generate SSH key pair
#   null       — wait for K8s cluster ready
#   local      — ghi SSH key va kubeconfig ra file
#
# Chay: make apply
# Xoa: make destroy
# ============================================================
