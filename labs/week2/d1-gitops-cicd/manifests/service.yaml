apiVersion: v1
kind: Service
metadata:
  name: nginx
  namespace: d1-gitops
  labels:
    app.kubernetes.io/name: nginx
    app.kubernetes.io/part-of: d1-gitops
spec:
  type: ClusterIP
  selector:
    app.kubernetes.io/name: nginx
  ports:
    - name: http
      port: 80
      targetPort: http
